/**Task 1:
 * Typed function
 * Create a function that takes a string and returns its length.
 */
function giveLength(str) {
    return str.length;
}
console.log(giveLength('harsh'));
console.log(giveLength('meetraj'));
